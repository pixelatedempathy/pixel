- `src/components/TodoReact.tsx` has unresolved JSX settings (`TS17004`) and missing `scheduler/tracing` types (`TS2307`).
- Despite resetting and reinstalling dependencies, the `jsx: "react-jsx"` option doesn't seem to apply correctly.

Action deferred for future debugging or configuration review.
